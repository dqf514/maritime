# MariOS 系统审查与修复总结（2026-09-15）

本次会话对 MariOS（`mos/`）进行了全系统审查（后端 / 前端 / UI/UX / 业务逻辑 / 部署），随后按 P0→P3 优先级完成了全部修复与航运业务对标增强。

**验证状态**：后端测试 54 → **222 passed（0 失败）**；前端 `npm run build` / `tsc --noEmit` / `eslint`（0 error）全部通过。所有金样例数值断言（TCE=9953.13、滞期费 6000.0 等）全程未破坏。未做 git commit。

---

## 一、审查阶段发现的主要问题（已核实）

### 严重（Critical）
- OAuth stub 后门默认开启：任意邮箱可免密登录任意租户 admin（`config.py:34` 默认 `True`）
- 模块许可证门禁默认全旁路（`license_dev_unlock="all"`），SaaS 授权体系失效
- JWT 默认密钥 `dev-secret-change-me`；每次启动自动灌 7 个已知口令 demo 账户
- 一次性登录令牌（magic link）在 API 响应明文返回并存入邮件日志
- `/uploads` 静态目录无鉴权，租户备份（含 PII）公开可下载
- 回收站快照向任意登录用户泄露 `password_hash`/`key_hash`/connector 明文 secret
- Office Graph token 明文落库；加密密钥直接复用 JWT 密钥
- 前端：JWT 存 localStorage 无 CSP、URL query 传 token、8 组演示密码硬编码进源码并明文回显

### 高（High）
- 租户封停/改密后旧 token 不失效；API Key 借用"最早用户"身份、scope 不生效
- 钱包扣费 check-then-act 并发超扣；已付款发票可改金额；负数/超额付款无校验
- 审批任意字符串=批准、可自审自批；权限矩阵 fail-open
- 全局港口表可被任一租户删改；CORS 通配+credentials；Excel 上传无限制
- 前端无 401 处理（token 过期被渲染成"降权"误导）；LookupSelect 缓存跨租户泄漏

### 中（Medium）
- 无登录限流；邮箱大小写不规范化可建双账号；跨租户 IDOR 引用
- 无 Alembic（手写 ALTER 补丁）；租户隔离无兜底无测试；零可观测性
- Laytime 条款（SHINC/SHEX）未实现、无时区；Email 同步/AI Hub 为 stub
- 金额 float 入参；N+1 查询；列表无分页上限；单号可碰撞

详细审查报告见 `mos/docs/internal/shipping-domain-review.md`。

---

## 二、P0 修复 — 阻断商用项（全部落地）

### 商务域
- **Charter 18 个条款字段化**：demurrage/despatch rate、laytime terms、C/P form、freight rate/basis、cargo_qty、装卸率、双佣金、hire 字段、交还船、ets_responsibility（`models_domain.py`）
- **佣金拆分**：address commission（扣收入）/ brokerage（计成本）分列，旧 `commission_pct` 向后兼容；新金样例 gold_02
- **TC hire**：`GET /charters/{id}/hire-summary`（gross − off-hire = billable × hire_per_day）
- **Off-hire 事件**：`off_hire_events` 表 + 开/关/列表端点 + 状态机

### 财务域
- **红冲**：`credit_notes` 表 + `POST /invoices/{id}/credit-note`，void 前置校验（CREDIT_NOTE_REQUIRED）
- **多币种**：Invoice `base_amount`/`fx_rate`，自动取 ExchangeRate；P&L 按本位币汇总
- **索赔**：timebar 自动推算（B/L date + 90 天）+ `days_to_timebar`；部分和解（settlement_amount）；`POST /claims/{id}/to-invoice`
- **hire 周期发票**：`POST /invoices/hire-schedule`（含 off-hire 扣减、address commission 净额）
- **BDN**：加油交付单字段 + 偏差 >2% 警告；ROB 守恒真实校验（0.5% 容差）
- **真 CII 公式**：`services/cii.py`（IMO MEPC.328/338/354 评级带）
- **风控**：`risk_limits` 表三级匹配（counterparty>symbol>global）替换硬编码 100000；制裁统一断言挂发票/付款/加油/portal + 审计表

### 运营域
- **SOF 标准码表**（12 码：NOR/EOSP/POB/COMMENDED 等）+ 时序校验 + 非法码 422
- **NOR/EOSP/B/L date 字段化**回写 PortCall；`GET /port-calls/{id}/sof-summary`；Port.holidays

### 前端接线
租约条款分组 + TC 卡片、红冲 UI、hire 发票入口、risk limits CRUD、off-hire 面板、SOF 下拉、索赔 days_to_timebar 红色预警等。

## 三、P1 修复 — 对标必需（全部落地）

- **估算引擎**：EU ETS 碳成本（`emissions_cost` 计入 voyage_cost）；多牌号油价（VLSFO/MGO 分档）；多腿航线（distance/speed 推天数）；金样例 +3（gold_03/04/05）
- **Laytime**：可逆/合并结算（reversible + average 两港互抵，金样例 gold_06）；定稿锁定（409）；结构化计算书导出；**`POST /laytimes/from-sof` 一键从 SOF 生成滞期计算**（对标 IMOS 核心流程）
- **COA lifting 生命周期**：planned→nominated→fixed→completed（nominate/fix/complete 端点）
- **Charter 变更单**：active 后关键字段禁 PATCH（409 AMENDMENT_REQUIRED），走 amendment 审批流（seq 递增、approve 生效）
- **P&L**：全行项（revenue/hire/demurrage/port/canal/bunker/commission/emissions/other）+ actual/accrual 双口径
- **燃油**：询比价子表（accept 回写订单价）；指数定价（SIN380+12 自动取 MarketQuote）；航次分摊估算
- **ETS 自动 eu_share**：按航线 EU 港进出自动推（EU↔EU=1.0、EU↔非EU=0.5）；charterer 分摊标记
- **船管**：证书到期自动迁移（<30 天 expiring）；drydock 自动占船期（幂等）
- **Dashboard**：KPI 大面积接真数据，演示数据全部打 `synthetic` 徽标

## 四、P2 修复 — 增强（全部落地）

- 估算：cargo_tolerance_pct（MOL ±%）、stowage_factor 超容/dwt 超重 warnings；金样例 gold_06
- Noon 天气字段（wind_bf/sea_state/current_kn）；SOF 豁免规则修正（EOSP/ANCHOR/POB 可早于 NOR）
- EEOI；FFA 纸货 vs 实货对冲视图（`GET /risk/hedge-view`）；付款冲正（`POST /payments/{id}/void`，含状态回写、GL 锁定）
- 工单-备件消耗（完工扣库存、幂等、不足告警）；船员 STCW 证书 + 60 天到期预警
- 补 GET 端点：bunker inquiries、COA liftings、invoice payments 列表

## 五、P3 修复 — 架构遗留（全部落地）

- **Cookie 会话改造**：HttpOnly + SameSite=Lax 双轨认证（Bearer/API key 兼容）；前端全面停止写 localStorage；`POST /auth/logout`；8 个专项测试
- **租户隔离门禁**：61 个攻击向量的越权矩阵测试套件（`tests/test_tenant_isolation.py`）；**顺带挖出并修复 11 处真实跨租户引用漏洞**（estimate/charter/voyage/laytime/claim/bunker 外键未校验，claim 还曾泄露外来 laytime 金额）；`services/tenant_guard.py` 防御层
- **可观测性**：X-Request-ID 纯 ASGI 中间件 + 结构化日志；`audit_logs` 表 + 登录/token/改密/用户管理审计点 + admin 查询端点
- **Alembic**：baseline 迁移（108 表实测可建）+ 过渡策略（`mos/apps/api/alembic/README.md`）
- **前端 UX**：`ConfirmDialog` 组件替换全部 window.confirm（含 Escape/焦点/alertdialog 语义）；`StateView` 三态容器修掉 trading/twin/analytics 加载失败同形；根除 analytics 800ms 竞态

### 安全加固明细（贯穿各波次）
- 默认配置全面收紧：oauth stub 默认关、许可证门禁默认开、种子数据默认关、JWT 密钥无默认（未配置生成随机密钥并告警）
- demo_token 从响应与邮件日志中彻底移除；备份移出静态目录；svg 上传禁止 + 魔数校验
- 回收站 admin-only + 快照/读取双重脱敏；Office token Fernet 加密落库（独立 ops_data_key + 版本位）
- Office OAuth state HMAC 签名 + 回调强制登录验签；登录/验证码端点 5 次/分限流
- 改密/禁用即吊销旧 token（pwv 机制）；API Key 绑定用户 + expires_at + scope
- 前端安全响应头（CSP/nosniff/X-Frame-Options/HSTS）；硬编码密码清除（改随机生成 + 一次性复制）
- 统一 401 拦截跳登录；LookupSelect 缓存加 TTL + 租户维度 + 登出清空
- 部署：依赖全部钉版、Dockerfile 非 root + HEALTHCHECK + fixtures 入镜像、`.dockerignore`、`npm ci`

---

## 六、测试与验证

| 阶段 | 后端测试数 | 说明 |
|---|---|---|
| 基线 | 54 | 修复前 |
| P0 | 136 | +安全 20、业务完整性 10、商务/运营/航运域等 |
| P1 | 165 | +估算/laytime/财务/船管 |
| P2 | 187 | +积载/EEOI/冲正/备件等 |
| P3 | **222** | +cookie 8、隔离矩阵 16、可观测性 11 |

前端：`npm run build` 57/57 路由通过；`eslint` 0 error（存量 warning 已降级标注）。

## 七、明确未做（建议下阶段排期）

- Postgres 生产化切换 + RLS（现 SQLite + 逐点过滤，已有隔离测试门禁兜底）
- 巨石组件拆分（finance/page.tsx 1700 行）、SWR/React Query 数据层、响应式重构、a11y 深度改造
- Email 同步 / AI Hub 真实实现（现为 stub，DDS 对外口径需对齐）
- 存量 lint warning（139 条）清理

---

*生成：Kimi Code CLI 会话，2026-09-15。审查报告详见 `mos/docs/internal/shipping-domain-review.md`。*
